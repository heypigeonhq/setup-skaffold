name: setup-skaffold

on:
    - push

jobs:
    setup-skaffold:
        name: Setup Skaffold

        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4.1.0

            - uses: ./

            - run: skaffold help
